{
  "name": "stream-relay-client",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "main": "client.js",
  "license": "MIT",
  "scripts": {
    "start": "node client.js",
    "bundle": "esbuild client.js --bundle --platform=node --outfile=client.bundle.js --minify"
  },
  "dependencies": {
    "bson": "^6.10.1",
    "undici": "^6.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.2"
  }
}
